import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  titleHeader: {
    marginTop: 16,
    marginBottom: 24,
    alignItems: "center",
    flexDirection: "row",
    gap: 14
  },
});
